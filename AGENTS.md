# AGENTS.md

用于学习与参与 `vitejs/devtools` 这类开源仓库的源码协作规范（也适用于你在本仓库做源码笔记/实验）。

## 语言

- 默认使用中文沟通与解释（除非你明确要求其他语言）。
- 面向开源社区的产出（Issue/PR 描述、代码注释、文档）以仓库既有语言与风格为准（通常为英文）；需要时可先给中文草稿，再转英文。

## 工作习惯

- 优先“小步快跑”：先复现/理解，再做最小修改；避免无关重构。
- 先读再改：改动前先定位入口/数据流/边界条件，并写下 2~3 条可验证结论。
- 任何不确定的设计取舍：优先开 Issue/Discussion 对齐方向，再写代码。

## 依赖与脚本

- 安装依赖优先使用 `pnpm`。
- 新增“生产依赖”（非 devDependency）前必须先征求你的确认。
- 运行/构建/测试命令以仓库 `package.json` 的 `scripts` 为准；不要凭经验硬写命令。

## 源码阅读路线（建议）

1. **从文档入手**：先读 `README`、`CONTRIBUTING`、`package.json` scripts、CI 配置（了解质量门槛）。
2. **画出结构图**：识别是否为 monorepo（常见 `packages/`、`playground/`、`examples/`、`docs/`）。
3. **找入口与主链路**：从“插件入口/启动入口/路由入口”追到“核心数据结构与协议”。
4. **选一个垂直功能切入**：例如某个面板、某个 Vite hook、某个 RPC/事件协议，顺藤摸瓜梳理调用链。
5. **再看边角**：错误处理、性能优化、缓存策略、跨版本兼容与 feature flags。

## 定位与调试方法

- 先用 `rg` 全局搜：入口导出、关键 hook、事件名、协议类型、UI 路由/状态管理关键字。
- 以“最小可复现”为核心：优先在仓库自带的 playground/example 里复现；没有就添加一个最小用例。
- 写日志要克制：优先走已有的 debug 日志机制（若存在），避免把临时日志混进最终提交。

## 贡献（PR/Issue）清单

- Issue：包含版本、复现步骤、期望/实际行为、最小复现仓库或代码片段。
- PR：描述清楚“动机 → 方案 → 影响面 → 验证方式”，并尽量附上截图/录屏（如果是 UI/交互）。
- 测试：能补就补；至少覆盖关键分支（成功路径 + 失败路径）。
- 兼容：关注 Vite/Node/浏览器支持范围与变更记录；避免引入破坏性行为。
- 文档：变更会影响外部使用方式时，同步更新文档与示例。

## Repository expectations

- 当你修改公共 utilities 的行为时，需要在 `docs/` 中补充说明。
